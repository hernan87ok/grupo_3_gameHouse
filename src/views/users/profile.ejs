<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head.ejs') %> 

<body>
      
    <%- include('../partials/header.ejs') %> 


    <main class="fixed">

        <div class="container" style="margin-top: 40px;">
            <div class="row">
                <div class="col-md-3">
                    <h2>Hola <%= user.firstName%> <%= user.lastName%> ! </h2>
                    <img src="/img/users/<%= user.avatar %> ">
                    <br>
                    <br>
                    <div class="alert alert-success"><%= user.email %> </div>
                    <br>
                    <div class="alert alert-success">Perfil: <% if(user.id_role==1) {

                        %>Cliente <%}else {%> 
                            Administrador <%}%>
                    
                    
                        </div>
                </div>
            </div>
        </div>

    </main>

    <%- include('../partials/footer.ejs') %> 

</body>


</html>